# 复制为 .env.development 后按需修改（不要提交含密钥的 .env）
# 仓库已提供 .env.development 模板；可按本机端口改代理目标。

# 后端代理目标（主站 /api，仅 dev server 使用，不会打进浏览器包）
# VITE_API_PROXY_TARGET=http://127.0.0.1:8088

# 无后端时：由 Vite 开发服务器直接返回 /api 假数据（data-search、新闻详情等）
# 设为 true 后会关闭 /api 代理，请勿与可用后端代理同时依赖
# VITE_USE_API_MOCK=true

# vLLM 开发代理（news-detail 等走 /llm/*，端口与 start_llm.sh 一致）
# VITE_VLLM_PROXY_TARGET=http://127.0.0.1:8004

# 【局域网必读】同事通过 http://你的IP:5173 访问时，必须设置本项，否则浏览器仍可能请求 127.0.0.1:5173（Vite 客户端 / 热更新）
# 复制为 .env.development.local 后填写，例如：
# VITE_DEV_PUBLIC_HOST=192.168.207.170

# 知识图谱子应用（5176）开发地址整段覆盖（一般不必设，iframe 会自动用「当前 hostname:5176」）
# VITE_KG_DEV_URL=http://192.168.207.170:5176/
# VITE_KG_DEV_PORT=5176
